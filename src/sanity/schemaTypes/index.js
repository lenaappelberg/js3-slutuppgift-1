import {blockContentType} from './blockContentType'
import { eventType } from './eventType'
import { featuresSection } from './feautureType'

export const schema = {
  types: [blockContentType,eventType,featuresSection],
}
